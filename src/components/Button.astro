---
interface Props {
  link: string;
  linkText: string;
}
const { linkText, link } = Astro.props;
---

<a href={link} target="_blank" class={'py-3 px-4 bg-buttonBg text-black hover:bg-buttonBgHover hover:text-paleLilac rounded-lg hover:text-primaryPink'}>
  {linkText}
</a>